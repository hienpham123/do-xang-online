const Footer = () => {
  return (
    <footer className="border-t border-slate-800 bg-slate-950/60">
      <div className="mx-auto flex w-full max-w-6xl flex-col gap-2 px-4 py-6 text-center">
        <p className="text-sm font-semibold text-slate-300">
          Xăng ảo nhưng cảm xúc là thật
        </p>
        <p className="text-xs text-slate-500">
          Demo vui nhộn, không cần backend.
        </p>
      </div>
    </footer>
  );
};

export default Footer;

